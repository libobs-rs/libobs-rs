[package]
name = "test_bootstrapping"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
libobs-bootstrapper = { path = "../../../libobs-bootstrapper" }
libobs = { path = "../../../libobs" }
tokio = { version = "1.49.0", default-features = false, features = [
    "macros",
    "rt-multi-thread",
] }

[build-dependencies]
libobs-bootstrapper = { path = "../../../libobs-bootstrapper" }
